<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin.baitapnhom4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity"
                  android:theme="@style/LoginThem">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuActivity"
                  android:theme="@style/LoginThem"/>
        <activity android:name=".com.example.admin.Menbres.MenbresActivity" />
        <activity android:name=".com.example.admin.Profil.Profil" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".ActualitesActivity" />
        <activity android:name=".AvantagesActivity"></activity>
    </application>

</manifest>